const playSound=e=>{const t=document.querySelector(`audio[data-key="${e.keyCode}"]`),n=document.querySelector(`.key[data-key="${e.keyCode}"]`);t&&(t.currentTime=0,t.play(),n.classList.add("playing"))},removeTransition=e=>{"transform"==e.propertyName&&e.target.classList.remove("playing")},keys=document.querySelectorAll(".key");keys.forEach(e=>e.addEventListener("transitionend",removeTransition)),window.addEventListener("keydown",playSound);